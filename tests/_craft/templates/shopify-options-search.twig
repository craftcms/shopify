{# Mirrors the README's `#### options` query-param example. #}
{% set products = craft.shopifyProducts.search('*yellow*').all() %}
{{- products|length -}}:{{- products[0] is defined ? products[0].shopifyGid : '' -}}
